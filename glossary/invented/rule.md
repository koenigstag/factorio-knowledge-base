# rule (in the context of relations/)

A derived numeric ratio obtained by applying a formula from
`formulas/` to specific `datapacks/` — e.g. "24 steel furnaces saturate
a yellow belt". Must be stored together with its derivation formula
and a source reference for verification, never as a bare constant with
no derivation path.

Defined during initial architecture discussion.
