# port

The point where a module connects to neighboring infrastructure:
resource, direction (in/out), side of the block, offset from the
corner, number of lanes, and its own inserter tier/count — not
inherited from the module as a whole (inserter throughput needs can
differ port to port even within one module).

Coined during initial architecture discussion.
