# export-block

A type of city-block module whose output doesn't return to the main
bus (like a regular processing block) but leaves in a separate
direction — onto a secondary bus or straight to rail export. Would
need different port geometry: the lower gap reserved for export
infrastructure instead of the next block in the same column.

Coined during initial architecture discussion; not yet implemented.
